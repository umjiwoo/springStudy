{{!src>main>resources>templates 경로에 머스테치 파일 생성 시 스프링부트에서 자동으로 로딩해줌}}
{{>layout/header}}
<h1>스프링부트로 시작하는 웹 서비스</h1>
<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            <a href="/posts/save" role="button" class="btn btn-primary">글 등록</a>
            {{!머스테치에서 userName을 사용할 수 있도록 IndexController에서 userName을 model에 저장해줘야함}}
            {{#userName}} {{!머스테치는 if문 지원하지 않고 true/false만 판단함;최종값을 넘겨주어 값 존재 여부에 따라 문장이 실행됨->#은 있을 때 ^는 없을 때}}
                Logged in as: <span id="user">{{userName}}</span>
                <a href="/logout" class="btn btn-info active" role="button">Logout</a>
            {{!스프링 시큐리티에서 로그아웃을 위한 url "/logout"을 지원해줌;컨트롤러 따로 만들 필요 x}}
            {{/userName}} {{!이름 있으면 "Logged in as:유저이름" 과 로그아웃 버튼이 보여지게 됨}}
            {{^userName}}
                <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
                {{!스프링 시큐리티에서 로그인을 위한 url "/oauth2/authorization/google"을 지원해줌;컨트롤러 따로 만들 필요 x}}
            {{/userName}}
        </div>
    </div>
    <br>
    <!--목록 출력 영역-->
    <table class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>게시글 번호</th>
            <th>제목</th>
            <th>작성자</th>
            <th>최종 수정일</th>
        </tr>
        </thead>
        <tbody id="tbody">
        {{#posts}} <!--posts 리스트 순회-->
        <tr>
            <td>{{id}}</td> <!--리스트에서 뽑아낸 객체의 필드 사용-->
            <td><a href="/posts/update/{{id}}">{{title}}</a></td>
            <td>{{author}}</td>
            <td>{{modifiedDate}}</td>
        </tr>
        {{/posts}}
        </tbody>
    </table>
</div>
{{>layout/footer}}